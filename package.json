{
  "name": "gabriel-souza-costa-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
